extends layout

block content
    form(class="form-container" method="POST" action="/new")
        label(for="user") User:
            br
            input(type="text" id="user" name="user" class="form-controls")
        label(for="text") Text:
            br
            textarea(id="text" name="text" class="form-controls")
        button(type="submit" class="form-controls") Submit Message